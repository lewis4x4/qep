/**
 * Embed QRM Edge Function (Tier 4 rename — Phase 2)
 *
 * New canonical name for embed-crm. Re-imports the same handler so the
 * embedding pipeline has one source of truth.
 */
import "../embed-crm/index.ts";
